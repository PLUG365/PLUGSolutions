# PLUG Solutions 掲載申請フォーム仕様 v1

## 目的と境界

- 作者本人から、公開カタログへ掲載するための情報だけを受け付ける。
- Microsoft Forms は「誰でも回答可能」にし、Microsoft アカウントへのサインインを要求しない。
- Forms が回答者の氏名やメールを自動取得しない設定にする。
- 公開用の表示名と X アカウントは入力を求めるため、完全な匿名投稿ではない。サイトでは「サインイン不要」と表記する。
- 本名、メールアドレス、会社名、所属、住所、電話番号、ユーザー ID、ファイルは収集しない。
- 機密情報、個人情報、顧客データを入力しないようフォーム冒頭と自由記述欄に表示する。
- 回答だけで自動掲載しない。SharePoint への転記、人による審査、公開 JSON の確認、Pull Request、本番リリースを必須とする。

## Forms 設定

| 設定 | 値 |
| --- | --- |
| フォーム名 | PLUG Solutions 掲載申請 |
| 回答者 | 誰でも回答可能 |
| サインイン | 不要 |
| 回答受付 | 有効 |
| 質問のシャッフル | 無効 |
| 開始・終了日時 | 初期は設定しない |
| ファイルアップロード | 使用しない |
| 回答のコピー送信 | 使用しない |
| 回答ごとのメール通知 | 運営者が必要と判断した場合のみ。回答内容や送信者メールを外部へ転送しない |

「1 人 1 回」の制限はサインインを必要とするため使用しない。重複や荒らしは審査で除外する。

## 冒頭表示

> 作者本人からの申請だけを受け付けます。氏名・メール・会社名は収集しません。公開用の表示名と X アカウント、作品情報は掲載時に公開されます。個人情報、顧客情報、社内 URL、API キーなどの秘密情報を入力しないでください。申請は掲載を保証するものではありません。

## 設問

設問 ID は Power Automate と SharePoint の対応付けで使用するため、作成後に意味を変更しない。回答負担を抑えるため、必須 7 問と任意 3 問の計 10 問にする。ライセンス、費用、Premium 要否、導入時間、前提条件は 1 問へまとめ、審査時に運営者が公開スキーマへ正規化する。

| ID | 設問 | 形式 | 必須 | 選択肢・制約 | 取扱い |
| --- | --- | --- | --- | --- | --- |
| C01 | 申請条件の確認 | 単一選択 | 必須 | 「作者本人または公開権限あり」「掲載への同意」「行動規範への同意」「秘密情報なし」「画像を出す場合は掲載権限あり」を説明文にまとめ、「すべて確認しました」のみ受理 | 非公開 |
| P01 | 作品名 | 短文 | 必須 | 80 文字以内 | `title` |
| P02 | 掲載する作者表示名 | 短文 | 必須 | 本名でなくてよい。40 文字以内 | `maker.displayName` |
| P03 | 公開 X アカウント | 短文 | 必須 | `@` から始まるハンドル。作者確認、公開連絡先、必要な審査連絡に使用 | `maker.xHandle`、`maker.xUrl` |
| P04 | 作品の概要 | 長文 | 必須 | 400 文字以内。何を解決するか、誰に役立つかを書く | `description` |
| P05 | 種類・主な用途 | 複数選択 | 必須 | Power Apps、Power Automate、Copilot Studio、Web、モバイル、AI、OSS、テンプレート、仕事効率化、暮らし・家計、学習、開発者ツール、その他 | `type`、`categories`、`tags` へ審査時に分類 |
| P06 | 配布先・利用先 URL | 短文 | 必須 | 公開済みの HTTPS URL | `distributionUrl` |
| P07 | ソースコード・導入手順などの関連 URL | 長文 | 任意 | 複数ある場合は 1 行に 1 URL | `sourceUrl`、`instructionsUrl` へ審査時に分類 |
| P08 | 利用条件・導入条件 | 長文 | 任意 | ライセンス、費用、Premium 要否、導入時間、必要環境・権限を分かる範囲で記載 | `license`、`cost`、`premiumRequired`、`setupTime`、`prerequisites` へ審査時に正規化 |
| I01 | サムネイル候補の公開画像 URL | 短文 | 任意 | HTTPS URL。自分に権利がある、または掲載許可を得た画像だけ。画像は自動取得しない | 候補 URL 自体は非公開 |

## Forms へコピペする内容

### フォームタイトル

```text
PLUG Solutions 掲載申請
```

### フォーム説明

```text
作者本人からの申請だけを受け付けます。

氏名・メールアドレス・会社名は収集しません。公開用の作者表示名、X アカウント、作品情報は掲載時に公開されます。

個人情報、顧客情報、社内 URL、API キーなどの秘密情報を入力しないでください。申請内容は人が確認するため、申請は掲載を保証するものではありません。
```

### 1. 申請条件の確認

- 種類：選択肢
- 必須：オン
- 複数回答：オフ

設問タイトル：

```text
申請条件の確認
```

説明：

```text
次のすべてを確認してください。

・自分が作品の作者、または公開する権限を持っています
・入力した掲載情報を PLUG Solutions 上で一般公開することに同意します
・PLUG の行動規範に同意します
・個人情報、顧客情報、機密情報、秘密情報を入力していません
・画像 URL を入力する場合、自分が権利を持つか、権利者から掲載許可を得ています
・審査で確認が必要な場合、入力した公開 X アカウントへ運営者から連絡してよいです
```

選択肢：

```text
すべて確認しました
同意できない項目があります
```

### 2. 作品名

- 種類：テキスト
- 必須：オン
- 長い回答：オフ

```text
作品名
```

サブタイトル：

```text
掲載時に表示する名称を入力してください。目安は 80 文字以内です。
```

### 3. 掲載する作者表示名

- 種類：テキスト
- 必須：オン
- 長い回答：オフ

```text
掲載する作者表示名
```

サブタイトル：

```text
本名でなくても構いません。サイト上に公開される名前です。
```

### 4. 公開 X アカウント

- 種類：テキスト
- 必須：オン
- 長い回答：オフ

```text
公開 X アカウント
```

サブタイトル：

```text
@ から始まるアカウント名を入力してください。作者確認、サイト上の作者リンク、必要な審査連絡に使用します。
```

### 5. 作品の概要

- 種類：テキスト
- 必須：オン
- 長い回答：オン

```text
作品の概要
```

サブタイトル：

```text
何を解決する作品か、誰に役立つか、どんな場面で使えるかを 400 文字以内を目安に記載してください。
```

### 6. 種類・主な用途

- 種類：選択肢
- 必須：オン
- 複数回答：オン
- 「その他」：オン

```text
種類・主な用途
```

サブタイトル：

```text
当てはまるものをすべて選択してください。
```

選択肢：

```text
Power Apps
Power Automate
Copilot Studio
Dataverse solution／PCF
Web アプリ
モバイル／デスクトップアプリ
AI ツール
OSS
テンプレート／部品
仕事効率化
暮らし・家計
学習
クリエイター支援
コミュニケーション
開発者ツール
```

### 7. 配布先・利用先 URL

- 種類：テキスト
- 必須：オン
- 長い回答：オフ

```text
配布先・利用先 URL
```

サブタイトル：

```text
作品を入手、利用、または確認できる公開済みの HTTPS URL を入力してください。
```

### 8. ソースコード・導入手順などの関連 URL

- 種類：テキスト
- 必須：オフ
- 長い回答：オン

```text
ソースコード・導入手順などの関連 URL
```

サブタイトル：

```text
GitHub、導入手順、紹介記事、動画などがあれば入力してください。複数ある場合は 1 行に 1 URL で記載してください。
```

### 9. 利用条件・導入条件

- 種類：テキスト
- 必須：オフ
- 長い回答：オン

```text
利用条件・導入条件
```

サブタイトル：

```text
分かる範囲で、ライセンス、費用、Power Platform Premium の要否、導入時間、必要な環境・権限・サービスを記載してください。不明な項目は空欄で構いません。
```

回答例：

```text
ライセンス：MIT
費用：無料
Premium：不要
導入時間：30 分程度
必要環境：Power Apps、SharePoint リスト作成権限
```

### 10. サムネイル候補の公開画像 URL

- 種類：テキスト
- 必須：オフ
- 長い回答：オフ

```text
サムネイル候補の公開画像 URL
```

サブタイトル：

```text
任意です。自分が権利を持つか、権利者から掲載許可を得た公開画像の HTTPS URL だけを入力してください。画像は運営者が確認・コピーし、メタデータ除去とサイズ調整を行います。未入力の場合は文字入りサムネイルを使用します。
```

## 送信完了メッセージ

> 申請ありがとうございます。内容、公開 URL、作者情報、ライセンス、導入条件、画像の権利を人が確認します。申請は掲載を保証するものではありません。確認が必要な場合は、記載された公開 X アカウントへ PLUG Solutions 運営者から連絡します。

## SharePoint 転記と非公開項目

- Forms の Response ID を一意キーとして SharePoint に保存し、Power Automate の再試行で重複作成しない。
- `C01`、`I01`、Response ID、回答日時、審査状態、審査メモは非公開とする。
- 公開 JSON は `P01`〜`P08` の承認済み情報から生成し、人が slug、分類、利用条件、正規化した URL、ローカルサムネイル、公開日、更新日を追加する。
- 回答にメール、氏名、秘密情報などの禁止情報が含まれていた場合は公開 JSON へ転記せず、審査状態を「要確認」または「却下」にする。

## サイト接続

1. Forms を作成し、「誰でも回答可能」でサインイン不要になることをシークレットウィンドウで確認する。
2. 回答用 URL を GitHub の `production` Environment variable `NEXT_PUBLIC_SUBMISSION_FORM_URL` に保存する。URL は公開情報なので Secret に保存しない。
3. デプロイワークフローで同名の環境変数を静的ビルドへ渡す。
4. 手動本番リリースを実行し、サイトの「サインイン不要のフォームから掲載申請」から Forms が開くことを確認する。
5. Forms URL が未設定または不正な場合は、サイトを「掲載フォーム準備中」のままにする。

## 公開前の人手確認

- シークレットウィンドウで Microsoft アカウントへのサインインを要求されない。
- 回答者の氏名とメールが Forms の回答列に自動追加されない。
- すべての必須同意が「はい」にならなければ受理しない。
- ファイルアップロード欄がない。
- フォーム本文、選択肢、完了メッセージに個人メールアドレスやテナント内部 URL がない。
- テスト回答を削除する前に Power Automate と SharePoint の重複防止を確認する。
